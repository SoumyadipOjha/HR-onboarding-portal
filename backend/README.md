# HR Onboarding Backend

This is the Express + MongoDB backend for the HR Onboarding Platform.

Quick start:

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies: `npm install`
3. Run in dev: `npm run dev`

Endpoints are defined under `src/routes`.
